<%- include('partials/header') -%> <%- include('navbar'); -%>
<div class="sel-margin">
  <div>
    <h1>Home</h1>
    <p><%= home %></p>
  </div>
  <%if (posts.length>0){%> 
    <% Array.from(posts).forEach(function(post){ %>
  <div>
    <h1><%=post.title%></h1>
    <p>
      <%= post.content.substring(0, 100)+"..." %><a href="/posts/<%=post._id%>"
        >Read More</a>
    </p>
  </div>
  <%})%> <%}%>
</div>
<%- include('partials/footer'); -%>
